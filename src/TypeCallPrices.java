public class TypeCallPrices {
    private Simple inside;
    private Simple outside;
    private Simple stationarNumber;

    public TypeCallPrices(Simple inside, Simple outside, Simple stationarNumber) {
        this.inside = inside;
        this.outside = outside;
        this.stationarNumber = stationarNumber;
    }
}
