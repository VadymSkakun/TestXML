public class TypeParameters {
    private Simple favouriteNumbers;
    private Simple tarificationType;
    private Simple connectionPrice;

    public TypeParameters(Simple favouriteNumbers, Simple tarificationType, Simple connectionPrice) {
        this.favouriteNumbers = favouriteNumbers;
        this.tarificationType = tarificationType;
        this.connectionPrice = connectionPrice;
    }
}
